#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	srand(time(NULL));
	char valid_pass[] = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
	 char password[password_lenght + 1];

	 for (int i = 0; i < password_lenght; i++
	{
		int rand_index = rand() % (sizeof(valid_pass) - 1);
		 password[i] = valid_pass[rand_index];
	}
	 password[i] = valid_pass[rand_index];

	 printf("Random password: %s\n", password);
	return (0);
}
